#include "main.h"
#include <stdio.h>

/**
* _strcat - concatenate strings
* @dest: the string 1
* @src: the string 2
* Return: concatenation
*/

char *_strcat(char *dest, char *src)
{
	strcat(dest, src);

	retrn (dest);
}
