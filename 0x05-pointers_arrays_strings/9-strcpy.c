#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcpy - print characters
 * @dest: the string
 * @src: the string
 * Return: a pointer
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
