#include "main.h"
#include <string.h>

/**
 * _strlen - give the legth of the string
 * @s: the string
 * Return: the legth of the string
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
