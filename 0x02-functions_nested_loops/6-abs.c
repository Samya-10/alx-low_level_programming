#include "main.h"

/**
 * _isalpha - print the alphabets
 * @n: the integer to give its absolute value
 * Return: void
 */

int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	} else
	{
		return (-n);
	}
}
