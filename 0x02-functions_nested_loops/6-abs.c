#include "main.h"

/**
 * _abs - gives the absolute value of number
 * @n: the integer to give its absolute value
 * Return: void
 */

int _abs(int n)
{

	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
